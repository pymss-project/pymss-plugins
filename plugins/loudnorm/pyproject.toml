[project]
name = "pymss-plugin-loudnorm"
version = "0.1.0"
description = "pymss plugin: LUFS loudness normalization"
requires-python = ">=3.10"
dependencies = [
    "pymss",
    "pyloudnorm",
]

[project.entry-points."pymss.plugins"]
loudnorm = "loudnorm"
